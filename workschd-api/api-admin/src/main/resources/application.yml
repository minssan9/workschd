--- #local 환경
spring:
  profiles:
    active: local
  profiles.group:
    "local"   : "default, api, actuator, swagger, security-api, database, logging, cache, google, secret"
    "develop" : "default, api, actuator, swagger, security-api, database, logging, cache, google, secret"
    "release" : "default, api, actuator, swagger, security-api, database, logging, cache, google, secret"