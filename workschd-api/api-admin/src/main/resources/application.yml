--- #local 환경
spring.profiles.group:
  "local"   : "default, api, swagger, security-api, database, logging, redis, secret"
  "develop" : "default, api, swagger, security-api, database, logging, redis, secret"
  "release" : "default, api, swagger, security-api, database, logging, redis, secret"